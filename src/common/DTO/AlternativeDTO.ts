type AlternativeDTO = {
    primaryId: number,
    primaryCode: string, 
    primaryScore: number,
    alternativeId: number,
    alternativeCode: string,
    alternativeScore: number,
    tsUpload: Date,
}

export default AlternativeDTO
